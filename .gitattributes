# Auto-detect text files and normalize to LF
* text=auto eol=lf

# Shell scripts must use LF
*.sh text eol=lf

# Binary files
*.png binary
*.jpg binary
*.pdf binary

# Don't diff lock files
uv.lock -diff
